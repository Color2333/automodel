# Meshy-AutoGLB 插件版本变更日志

## 版本 4.5.8 - 20260514

### 修复 - 20260602
- **修复导出半修复状态**：Fixed 导出时强制按 Blender 当前求值结果导出 modifier，并自动跳过隐藏物体与 LogoSlot 布尔辅助 Cube；导出居中时会临时同步移动 Boolean modifier 引用的 operand，避免未 Apply 的布尔对象与模型错位后导出成局部半修复。
- **过滤修复工具辅助物体**：导出时会识别 `logoslot_boolean_helper` 标记，配合 Logo Slot Boolean Fixer 自动隐藏 Apply 后的布尔物体，避免槽内残留辅助 Cube。
- **布尔材质误过滤修复**：导出时不再仅凭 `LogoSlot_Boolean_Cube_Material` 材质槽跳过整个 Mesh，避免 Boolean Apply 后模型本体继承布尔材质槽而被误判为辅助物体，导致提示「没有找到包含网格的组」。
- **修复缺漏核对**：修正最终导出记录扫描缩进，确保 `核对导出缺漏` 能正确读取 NoLogo / Fixed / HardFix 输出文件。
- **按组导出失败回滚**：按组导出时任一有效组失败会取消整次导出，并清理本次已写出的临时成功组，避免部分组成功却把模型标记为 Fixed。
- **提交雕刻/编辑数据**：Fixed 导出前会强制 `flush_edits` 并从 Sculpt/Edit 等模式切回 Object，避免仍停留在编辑模式时导出器读取到未提交或不稳定的网格数据。
- **自动恢复保护**：插件启动自动恢复进度时，如果当前 `.blend` 场景已经包含模型几何，会跳过自动重新导入源 GLB，避免覆盖未导出的修复场景。

### 功能更新
- **HardFix 归档 Tag**：难以修复归档拆分为「原始模型异常」「零部件类」「难以修复」「其他」四类 Tag，仍统一输出到 `HardFix_{操作者}`，便于后续按归档原因统计。
- **HardFix 子目录归档**：`HardFix_{操作者}` 下按 Tag 自动创建「原始模型异常」「零部件类」「难以修复」「其他」子文件夹，归档 GLB 直接写入对应子目录；重新归档同一模型时会清理其他 Tag 子目录中的旧文件。
- **其他理由必填**：选择「其他归档」时必须填写理由；前三类标准 Tag 不再强制填写自由文本。
- **报告字段补充**：`logo_slot_report_*.csv` 增加 `archive_tag` 字段，`reason` 对标准 Tag 写入 Tag 名，对「其他」写入人工理由；缺漏核对 CSV 同步输出归档 Tag。
- **进度兼容**：`progress.json` 新增 `t` 字段保存归档 Tag，旧进度无该字段时默认恢复为「难以修复」。

## 版本 4.5.7 - 20260513

### 功能更新
- **导出缺漏核对工具**：小功能工具中新增「核对导出缺漏」，对比源目录 GLB 与 `NoLogo_{操作者}` / `Fixed_{操作者}` / `HardFix_{操作者}` 三个最终目录，生成 `Reports_{操作者}/logo_slot_missing_check_*.csv`，列出缺漏模型、跨分类重复模型和无源导出文件，方便收尾核数。
- **GLB 导出拓扑保持**：法线导出新增「自动 / 不导出 / 导出」三档；默认自动跟随源 GLB，源文件无 normals 时不导出 normals，避免 Blender glTF exporter 因硬法线把 manifold 已连通网格重新拆成按面分裂的顶点记录。
- **污染法线识别**：法线自动模式现在会识别旧导出文件中「无 UV 且 POSITION 数量接近 index 数量」的 Blender 拆点特征，即使文件里已有 normals，也会关闭 normals 导出，避免继续传播污染法线。
- **GLB 导出 UV 保持**：UV 导出新增「自动 / 不导出 / 导出」三档；默认自动跟随源 GLB，源文件无 UV 时不导出 UV，避免手动布尔使用 Cube/Cylinder 等辅助物体后把默认 UV 带入无 UV 模型。

### 性能优化
- **按组导出 I/O 合并**：`TOP_LEVEL` 按组导出时，CSV 报告更新和 `progress.json` 保存从“每个组一次”改为“整次导出结束后一次”，减少大量重复读写和目录扫描，不改变 GLB 导出内容。
- **导出所有去重**：`ALL_GROUPS` 收集对象时增加去重，避免复杂层级下重复选择对象造成额外开销。
- **输出目录解析缓存**：按组导出期间复用同一次输出目录解析结果，减少共享盘/NAS 上重复写入 probe 文件的开销。
- **二次保存保护**：终态 GLB 导出不再在导出前清空旧文件，改为先写临时 GLB，导出成功后替换并清理旧副本；避免“已修复”二次保存失败时把第一次保存的文件删掉。

## 版本 4.5.6 - 20260430

### 功能更新
- **Logo / Slot 专用流程**：将主流程收敛为「无需修复」「可修复」「已修复」「难以修复」；「可修复」作为中间状态，修复完成后导出为 `Fixed_{操作者}`。
- **输出命名规则**：新增 `NoLogo_{操作者}`、`Fixed_{操作者}`、`HardFix_{操作者}`、`Reports_{操作者}`；GLB 文件使用 `nologo__`、`fixed__`、`hardfix__` 前缀，按组导出使用 `__part001` 连续编号。
- **难以修复原因与 CSV**：难以修复必须填写原因；最终状态会写入 `Reports_{操作者}/logo_slot_report_{操作者}.csv`，并按源文件路径去重更新。
- **去重保护**：重新标记或重新导出同一模型时，会先清理三类最终输出目录中的旧文件；目标文件名冲突时追加源路径短哈希，避免同名模型覆盖。

## 版本 4.5.5 - 20260423

### 修复
- **前缀同名模型误删/误移**：重写 `list_model_glbs_in_dir` 的文件匹配规则，修复当两个模型名存在前缀包含关系（例如 `k3dp_…PORTRAIT` 与 `k3dp_…PORTRAIT ONE PIECE`）时，后打标签的短名模型会误删/误移先前长名模型输出 GLB 的问题。具体改动：
  - 移除旧的 `glob.escape(key) + "*.glb"` 兜底分支——它会把任何以当前模型名开头的文件（包括同前缀的其他模型）都当成本模型的旧副本，是 `purge_terminal_exports_for_model`、`purge_completed_exports_for_model`、`move_completed_to_category` 三处共同的"前一个模型消失"根因。
  - 收紧 `{model}_` 后缀规则为 `{model}_{纯数字}.glb`，与 `export_objects` 生成的 `f"{model.name}_{group_index}"` 严格对齐；避免像 `foo` vs `foo_big_1.glb` 这种同前缀但非按组导出索引的文件被误判。
  - `move_completed_to_category` 的 LEP（last_export_path）兜底也对齐新规则：仅在 LEP 指向 Completed 内且文件名严格匹配时把该单文件作为源，不再按宽松规则重扫目录。
- 兼容性：`progress.json` 与各分类目录结构未变更；打标/导出/按组导出/从 Completed 移动的主流程行为保持不变。

## 版本 4.5.4 - 20260420

### 功能更新
- **可修复后可切换下一项**：标记为「可修复」后允许点击「下一项」继续给其他模型打标，与重新打终态标签后的自动跳转逻辑区分；「上一项」在可修复中仍禁用，侧栏提示同步更新。

### 性能优化
- **导入加速**：`import_scene.gltf` 新增 `import_pack_images=False`，大文件（>60 MB）不再将贴图打包进 .blend，显著减少导入时间和内存占用；新增 `guess_original_bind_pose=False`，跳过骨骼绑定姿态推算（插件会在导入后清掉空骨骼，此计算对静态模型无意义）。
- **导出加速**：`export_scene.gltf` 新增 `export_image_format='AUTO'`，保留贴图原始格式（JPEG/PNG），避免重新编码，对大文件导出提速明显。
- **导航加速**：移除切换模型时冗余的 `orphans_purge` 和 `view_all` 调用（`import_model` 内部已执行），减少每次切换的重复开销。
- **导出加速**：`export_objects` 中父级查找改用 `set` 替代 `list`，O(n²) → O(n)，对象数量多时（>几十个）避免重复线性扫描。

## 版本 4.5.3 - 20260401

### 修复
- **加载模型后残留**：此前用「全选 + 删除」清理场景，会漏删不可选对象、以及 glTF 导出器生成的 `glTF_not_exported` 集合内的物体等；现改为按 `scene.objects` 逐个移除数据块，并删除空子集合、执行孤儿数据清理，切换/重载模型后不应再留下棱角球等附加网格。

### 功能更新与改进
- **进度自动恢复**：`progress.json` 中每条模型除 `n/p/s` 外增加 `h`（导出历史）、`lx`（上次导出状态），保存/加载进度时一并读写；重开 Blender 或手动「加载进度」后可还原侧栏历史。若旧进度无 `h` 且为终态，仍尝试从分类目录下 `{模型名}.json` 侧车恢复。
- **导航**：「未标记」时允许「上一项」、禁止「下一项」；「可修复」时禁止「上一项/下一项」（须先导出 Completed 并完成终态分类）；侧栏按钮与快捷键行为一致。
- **重新打标签唯一输出**（延续 4.5.2）：标记终态前清理各终态目录及非移动路径下 Completed 中该模型旧 GLB，保证输出侧单份结果。

## 版本 4.5.2 - 20260331

### 功能更新与改进
- **修复与分类工作流**：
  - 标为「可修复」后先在场景中修复，再使用「按组导出 / 导出所有」将 GLB 写入 `Completed_{操作者}`。
  - 点 good / bad / 存疑 / hard（及零件）终态时，将 Completed 目录下本模型对应的 GLB（含 `模型名_N.glb` 多组）**移动**至分类目录，磁盘上仅保留分类侧一份文件。
  - 未进入「可修复」的初筛路径：直接点终态仍为「导出至分类目录 + 自动下一项」。
- **导出与输出目录**：
  - 任意已标记状态均可手动导出 GLB（仅「未标记」不可导出）；废除标记后自动写入 JSON 记录。
  - good / bad 对应输出文件夹为 `Good_{操作者}`、`Bad_{操作者}`（原 NoAction_/Unfixable_ 不再写入）；新增 **hard** 状态与 `Hard_{操作者}` 目录。
- **界面**：
  - 状态按钮文案：good、bad、存疑、hard、可修复；当前已为可修复时禁用再次点击「可修复」。
  - 导出区域增加说明：先导出到 Completed，再点分类。
- **兼容**：
  - `progress.json` 内状态枚举 ID 未变（仍为 NO_ACTION、UNFIXABLE 等）。
  - 加载进度时，对旧版 `NoAction_*` / `Unfixable_*` 下的侧栏 JSON 保留可选回退查找。
- **流程约束**：状态为「可修复」时禁止通过上一项/下一项（含 Ctrl+方向键）切换模型，须先完成导出与分类；侧栏导航按钮同步禁用并提示。
- **修复**：Completed 内查找待移动 GLB 时增加 Unicode NFC 归一化、放宽 `模型名_*` 后缀匹配，并在仍找不到时用「上次导出路径」回退，减少长文件名/含下划线模型名或系统编码差异导致的误报「未找到 GLB」。
- **修复**：「导出所有」原先误生成 `模型名_None.glb`（因传了 `group_name` 但未传 `group_index`），与移动逻辑期望的 `模型名.glb` 不一致；现仅在存在 `group_index` 时使用 `_序号` 后缀，合并导出为 `模型名.glb`。
- **调整**：good / bad 输出目录改为首字母大写 `Good_{操作者}`、`Bad_{操作者}`（与 `Completed_`、`Questionable_` 风格一致）；
- **重新打标签（唯一输出）**：标记终态前先从 Good/Bad/Questionable/Hard/Parts 删除该模型全部匹配 GLB；从非「可修复」路径导出终态前另清 `Completed` 中该模型陈旧 GLB。从「可修复」移动至终态时仍以 Completed 为源，不在移动前清空 Completed。从终态改回「可修复」时删除各终态目录中该模型文件。

## 版本 4.50 - 20250308

### 功能更新与改进
- **旋转逻辑优化**：
  - 将旋转计算从欧拉角改进为四元数实现，解决万向节锁问题
  - 优化旋转操作使用世界坐标系，确保多次旋转行为更加一致和可预测
  - 连续旋转不同轴向时，每次都严格按照世界坐标系进行，避免坐标系混乱
  - 添加详细的代码注释，提升代码可维护性
  - 优化提示信息，明确指出旋转是在世界坐标系下进行的
  - 解决在多次连续旋转不同轴向时可能出现的异常行为

## 版本 4.49 - 20250307

### 功能更新与改进
- **界面布局优化**：
  - 将进度管理面板移至界面最底部，优化从上到下的操作逻辑
  - 简化模型旋转控制区域，改为两列三行布局
  - 每个轴向只保留90°和180°旋转选项，移除负角度旋转
  - 使用更直观的按钮标签"X轴 90°"等，移除不必要的图标
  - 整体界面更加简洁，操作更加高效直观

## 版本 4.48 - 20250307

### 功能更新与改进
- **界面操作优化**：
  - 将小功能工具区域从弹出式子面板改为直接折叠面板
  - 使用下拉箭头控制面板展开/折叠，操作更加直观
  - 保留原有工具功能，优化布局和空间利用
  - 减少点击层级，提高操作效率

## 版本 4.47 - 20250307

### 功能更新与改进
- **轴向颜色优化**：
  - 改进XYZ轴的颜色标识，使用Blender原生轴向图标
  - 移除X轴的红色文本，与Y轴和Z轴保持一致的样式
  - X轴使用AXIS_FRONT图标，保持红色视觉标识
  - Y轴使用AXIS_SIDE图标，保持绿色视觉效果
  - Z轴使用AXIS_TOP图标，呈现蓝色视觉效果
  - 优化图标排列和按钮布局，提升操作效率

## 版本 4.46 - 20250307

### 功能更新与改进
- **旋转控制UI优化**：
  - 简化180度旋转控制，移除不必要的正负区分
  - 优化XYZ三轴的颜色标识，使用红绿蓝直观区分
  - 采用网格式布局，四列整齐排列，便于快速定位
  - 移除冗余的旋转方向图标，只保留必要的数字文本
  - 统一控制布局风格，操作更加一目了然

## 版本 4.45 - 20250307

### 功能更新与改进
- **用户体验优化**：
  - 将模型旋转控制功能从小功能工具面板移至主界面
  - 放置于进度管理区域下方，提供即时访问，无需打开子面板
  - 保留轴向颜色编码和清晰的按钮布局，增强视觉识别性
  - 提升高频操作的可访问性，减少操作层级

## 版本 4.44 - 20250307

### 功能更新与改进
- **新增模型旋转控制**：
  - 添加便捷的模型旋转控制区域，支持XYZ三轴的精确旋转
  - 提供±90度和±180度快速旋转按钮，一键完成模型调整
  - 采用颜色编码（X轴红色）直观区分不同轴向
  - 整合图标式按钮，方向一目了然，提升操作效率


## 版本 4.43 - 20250307

### 功能更新与改进
- **界面优化**：
  - 将小功能工具区域改为下拉式弹出面板，减少界面占用空间
  - 保持每个功能按钮单独一行，提升操作清晰度
  - 优化整体UI布局，使主界面更加简洁


## 版本 4.42 - 20250307

### 功能更新与改进
- **小功能工具区域扩展**：
  - 添加"清除空组"功能，递归清除所有不包含网格对象的空组


## 版本 4.41 - 20250307

### 功能更新与改进
- **小功能工具区域扩展**：
  - 新增"小功能工具"专用区域，集中管理实用辅助功能
  - 添加"清理数据块"功能，一键清理所有未使用数据，提升性能
  - 添加"选中描边物体"功能，快速选中所有使用含"outline"材质的物体
  - 添加"清除空组"功能，递归清除所有不包含网格对象的空组
  - 重新组织去除Alpha功能，使其更符合工作流程

- **性能优化**：
  - 改进数据清理逻辑，减少内存占用
  - 优化模型导入和导出过程，提高大型项目的处理效率
  - 完善错误处理和状态反馈，提供更直观的操作结果

- **UI布局优化**：
  - 调整功能区块布局，使界面结构更加清晰
  - 根据使用频率和逻辑关系重新组织功能按钮
  - 统一按钮样式和尺寸，提升视觉一致性

## 版本 4.30 - 20250306

### 功能更新与改进
- **UI界面优化**：
  - 优化导航控制区域，显著增大导航按钮尺寸，提升操作便捷性
  - 改进状态显示和视觉反馈，更适合长期处理海量数据
  - 将进度管理功能改为下拉面板形式，使界面更加简洁
  - 移除多余的进度管理按钮，仅保留必要功能

- **新增功能**：
  - 添加"去除Alpha"功能，一键断开所有材质中连接到Alpha的节点，使模型完全不透明
  - 添加"清理数据块"功能，一键清理场景中所有未使用的数据块，释放内存
  - 新增"小功能工具"区域，集中管理实用小工具
  - 自动处理常见的透明度连接方式，包括原理化BSDF、输出节点和混合着色器

- **用户体验提升**：
  - 简化组创建流程，不再弹出命名对话框，加速工作流程
  - 优化按钮布局和大小，减少长期操作时的用户疲劳
  - 重命名部分按钮，更准确反映其功能，避免混淆

## 版本 4.2.9 - 20250305

### 功能更新与改进
- **UI界面优化**：
  - 将"完成"状态按钮文本更改为"可修复"，避免用户对功能产生歧义
  - 对应调整状态显示文本，保持UI一致性
  - 改进用户体验，提供更直观的操作引导

- **模型组织优化**：
  - 将组显示从盒子(CUBE)改为纯轴十字架(PLAIN_AXES)，优化视觉表现
  - 优化"按组导出"功能，导出的文件现在使用连续的数字后缀，更加直观
  - 移除"完成"状态导出后自动切换到下一个模型的行为，允许用户完成最终检查

## 版本 4.2.7 - 20250305

### 功能更新与改进
- **导出功能升级**：
  - 移除"导出选中"按钮和功能，简化操作流程
  - 新增"导出所有"功能，可将所有包含网格的组合并为一个GLB文件导出
  - 改进"按组导出"功能，智能跳过没有网格对象的组或节点，防止空导出
  
- **模型切换优化**：
  - 在模型切换时自动清除撤销历史，防止用户意外通过Ctrl+Z回到上一个模型
  - 增强导出逻辑的稳定性和性能
  - 完善用户体验，提供更清晰的操作反馈

## 版本 4.2.6 - 20250305

### 功能优化
- **自动读取用户名**：
  - 改进源目录设置逻辑，现在会自动从进度文件中读取并应用用户名
  - 同一源目录可以在不同会话间自动应用相同用户名，无需手动设置
  - 完善进度文件解析，同时兼容新旧格式的JSON文件结构

## 版本 4.2.5 - 20250305

### 界面优化
- **UI布局调整**：
  - 调整状态标记区域，"完成"按钮独立一行并放大，更加醒目
  - 重新排序UI元素，按照工作流程顺序排列：状态标记 -> 模型组织与导出 -> 导航控制
  - 增大导航箭头按钮，提升操作体验
  - 优化各功能区域标题和图标，视觉效果更清晰

## 版本 4.2.4 - 20250305

### 修复
- **重要修复**：
  - 修复标记为"存疑"、"无法修复"或"无需操作"状态后，虽然索引变化但实际模型不会切换的问题
  - 现在自动标记状态后会正确清理场景并导入下一个模型，恢复自动化工作流程
  - 巩固之前的错误修复，确保完整的工作流程执行

## 版本 4.2.3 - 20250305

### 修复
- **紧急Bug修复**：
  - 修复点击"存疑"、"无法修复"或"无需操作"时模型不会切换的问题
  - 修复重复导出JSON的问题
  - 修复枚举值不匹配导致无法正常设置状态的错误

## 版本 4.2.2 - 20250305

### 性能优化
- **进度文件极简化**：
  - 优化progress.json文件结构，大幅减小文件体积
  - 只保存最基本的模型信息(名称、路径、状态)
  - 详细的导出历史和状态数据从各模型独立JSON中读取
  
- **适应大规模模型场景**：
  - 针对处理上千个模型的情况进行性能优化
  - 减少频繁读写大文件的性能压力
  - 完全兼容现有工作流程和功能

## 版本 4.2.1 - 20250305

### 优化功能
- **优化导出流程**：
  - 只有"完成"状态需要用户手动导出GLB文件
  - "无需操作"、"存疑"和"无法修复"状态在标记后自动导出JSON并切换下一个模型
  - 每个模型只输出一个对应的JSON文件，自动保存到对应状态的文件夹

- **增强用户体验**：
  - 简化操作流程，减少重复点击
  - 提供更清晰的状态反馈提示
  - 标记状态后自动执行相应的导出操作

## 版本 4.2.0 - 20250305

### 新增功能
- **新增"无需操作"状态**：
  - 添加第四种状态标签"无需操作"，表示不需要处理的模型
  - 优化状态标签的显示和操作流程
  
- **根据状态智能导出**：
  - "无需操作"状态的模型导出为GLB文件
  - "完成"、"存疑"和"无法修复"状态的模型只导出JSON记录
  - 每种状态对应单独的子文件夹，便于后期管理和回收

- **优化用户体验**：
  - 选择"无需操作"、"存疑"或"无法修复"状态后自动切换到下一个模型
  - 调整UI布局顺序，状态标记按钮位于顶部，导航部分位于其下方

### 优化
- **界面改进**：
  - 状态标签使用不同颜色和图标更直观地表示
  - 简化工作流程，减少重复操作

## 版本 4.1.5 - 20250305

### 修复
- **解决启动崩溃问题**：
  - 修复`'_RestrictContext' object has no attribute 'scene'`错误
  - 移除注册函数中不安全的场景访问代码
  - 优化启动顺序，确保在合适的时机初始化

- **增强启动稳定性**：
  - 为所有关键操作添加更完善的错误处理
  - 添加详细的日志输出，便于排查问题
  - 确保插件在各种启动条件下都能安全加载

## 版本 4.1.4 - 20250305

### 修复
- **彻底解决版本号显示问题**：
  - 完全重写版本号显示代码，硬编码当前版本解决显示问题
  - 确保UI中始终显示正确的插件版本号(4.1.4)
  - 解决插件注册后还保留旧版本设置的问题

- **增强缓存清理**：
  - 插件注册时立即强制清理所有设置和缓存
  - 所有属性都添加`SKIP_SAVE`选项，防止Blender保存和恢复
  - 采取更激进的方法确保每次安装插件后都从干净状态开始

### 提示
- 如果依然有问题，建议尝试以下解决方法：
  - 使用Blender的factory settings启动(编辑 -> 首选项 -> 保存与载入 -> 出厂设置)
  - 手动删除Blender配置目录中的插件相关文件
  - 在全新的Blender实例中安装插件

## 版本 4.1.3 - 20250305

### 改进
- **强化缓存清理**：
  - 彻底解决插件缓存和设置问题，确保每次启动完全重置
  - 强制版本号显示正确，不再依赖Blender缓存
  - 所有设置(包括目录、用户名)都不再保存到Blender配置中

### 修复
- **版本显示问题**：
  - 修复UI界面版本号与实际安装版本不一致的问题
  - 确保重装插件后版本号显示正确

## 版本 4.1.2 - 20250304

### 改进
- **启动行为优化**：
  - 修改启动行为，确保每次启动时都需要手动选择源目录
  - 选择目录后自动检测该目录的进度文件并恢复用户进度
  - 修复版本号显示不正确的问题，确保显示实际安装的版本

## 版本 4.1.1 - 20250304

### 改进
- **命名优化**：
  - 在"按组导出"模式下，多个模型采用简洁的数字后缀命名方式
  - 文件名现在使用"模型名_1.glb"、"模型名_2.glb"等格式，而不是添加完整的组名
  - 提高了导出文件名的清晰度和一致性

## 版本 4.1.0 - 20250304

### 新增功能
- **改进的进度保存机制**：
  - 进度文件现在保存在源目录的`.progress`文件夹中，而不是用户主目录
  - 一个源目录只对应一个进度文件，避免多人使用的冲突
  - 插件卸载重装后仍能自动找到之前的进度

### 改进
- **用户体验优化**：
  - 现在只需设置源目录，即可自动检测是否存在之前的进度
  - 如果存在进度文件，会自动提取用户名并应用
  - 用户界面提示更加清晰，引导用户完成必要的设置

## 版本 4.0.0 - 20250304

### 新增功能
- **改进的层级导出支持**：
  - 现在支持导出嵌套多层级的结构，无论层级有多深
  - 顶级节点下的所有子对象(包括空物体)会被完整保留在导出文件中
  - 保留所有嵌套的父子关系，确保模型结构完整

### 改进
- **简化导出流程**：
  - 移除了导出时的模式选择对话框，直接采用"按组导出"模式
  - 将"按顶级节点"重命名为更直观的"按组导出"
  - 优化了位置调整算法，只移动顶级对象，子对象自动跟随

## 版本 3.0.0 - 20250304

### 新增功能
- **模型组织功能**：
  - 新增"将选中对象创建为组"功能，可将多个网格对象组织在同一个父节点下
  - 支持按组批量导出模型，自动处理每个顶级节点及其子物体
  - 导出文件名会自动包含组名，便于管理

- **进度保存与恢复**：
  - 添加进度自动保存功能，可保存当前处理状态、模型标记和导出历史
  - 用户重新打开Blender后可自动恢复上次的处理进度
  - 进度文件按用户名和源目录隔离，避免多用户间冲突
  - 提供手动保存、加载和清除进度的选项

- **处理流程管理**：
  - 实现强制工作流程，确保每个模型都经过标记和导出步骤
  - 自动跟踪模型的标记状态和导出状态
  - 防止用户跳过未完成处理的模型，提高工作规范性

### 改进
- **导出逻辑优化**：
  - 重构了导出代码，避免创建多余的骨骼数据
  - 导出时保持模型的父子关系，只调整位置
  - 简化了文件命名规则，确保一致性

- **用户界面改进**：
  - 新增进度管理区域，显示保存状态
  - 调整状态标记按钮，移除快捷键显示
  - 添加更详细的状态提示信息

### 修复
- **导出逻辑优化**：
  - 修复了嵌套结构导出时可能丢失中间层级的问题
  - 优化了中心点计算逻辑，现在只基于网格对象计算中心点
  - 改进了导出后的位置恢复逻辑，确保场景不会被意外修改

- **导出修复**：
  - 修复导出后重新导入模型时出现额外空骨骼的问题
  - 优化对象选择逻辑，确保只导出选中的模型
  - 解决导出过程中可能导致的模型位置变化问题

- **代码优化**：
  - 为关键操作添加异常处理
  - 优化文件路径处理，支持更长的路径名
  - 改进父级-子级关系处理逻辑

### 移除
- 移除了数字键(1,2,3)用于标记状态的快捷键
- 移除了强制跳过未处理模型的选项

### 注意事项
- 本版本改变了处理工作流程，用户必须先标记模型状态，再导出模型后才能进入下一个模型
- 进度文件保存在源目录的.progress文件夹中
- 请确保使用最新版本的Blender (4.0+)以获得最佳体验 
